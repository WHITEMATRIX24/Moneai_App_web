import mongoose from "mongoose";const schema=new mongoose.Schema({userId:{type:mongoose.Schema.Types.ObjectId,ref:"User",required:true,unique:true},plan:{type:String,enum:["FREE","PREMIUM"],default:"FREE"},status:{type:String,enum:["ACTIVE","CANCELLED","EXPIRED"],default:"ACTIVE"},startedAt:{type:Date,default:Date.now},expiresAt:Date},{timestamps:true});export default mongoose.model("Subscription",schema);
